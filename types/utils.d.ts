export declare function makeNewCanvas(width: number, height: number): HTMLCanvasElement;
export declare function hasAlpha(data: Uint8ClampedArray, x: number, y: number, width: number): boolean;
export declare function parseOptions(options?: Options): {
    padding: number[];
};
